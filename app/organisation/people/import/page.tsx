import { ImportClient } from "@/components/people/ImportClient";

export const dynamic = "force-dynamic";

/** vCard import with the review list (people-contacts C2). */
export default function PeopleImportPage() {
  return <ImportClient />;
}
