# Mycelium — Master Context Doc

> **How to use this doc**
> Paste the whole thing into Claude at the **start** of a Mycelium session, before real work begins. This file is the **single source of truth** — it overrides anything stale in Claude's built-in memory. At the **end** of a session, Claude hands you a `DOC UPDATE` block (ADD / REPLACE / REMOVE with exact text + section); you apply the edits here so the doc stays current.
>
> Keep it sectioned so edits stay surgical. Prune freely — shorter and accurate beats long and stale.
>
> _Last updated: 9 June 2026_

---

## 1. Identity & setup

- **Phil** — Armthorpe / Doncaster, **DN3**, Europe/London.
- **Mycelium** — personal AI dashboard at **mycelium.sporebit.com**. Deployed and live.
- **Stack:** Next.js 15, React, Supabase (hosted Postgres), Vercel. Capture via Telegram bot (inbound) + iOS Shortcuts → `/api/capture-audio`.
- **Repo:** github.com/sporebit/mycelium · **Local:** `P:\Projects\Mycelium`

---

## 2. Tooling & workflow

- Supabase CLI via Scoop; migrations applied with `supabase db push` from project root. **Latest migration: 0050** (`0050_spend_analytics.sql` — spend_by_category + spend_by_month RPCs).
- PowerShell `mycelium` function cds to project + runs `git status -sb`.
- `AGENTS.md` instructs Claude Code to **auto-commit + push to main** after every code-changing task.
- One commit per concern (clean atomic history). Parallel Claude Code sessions for non-conflicting work.
- Token-efficient prompts — no boilerplate, no "per AGENTS.md" reminders. "yooooo" sound via Claude Code Stop hook in `.claude/settings.json`.
- **Shell command formatting:** multi-line blocks (git, curl with line continuations) end with a trailing newline so they paste-and-go without a manual Enter.
- **Migration numbering by BUILD ORDER** — assign the next available number at build time; out-of-order numbers cause `db push` failures.
- **External scheduler: cron-job.org** (free) — used for sub-daily cron triggers on Vercel Hobby plan (Hobby caps Vercel cron at once/day; sub-daily expressions fail the deploy). Configure with `Authorization: Bearer` header. Vercel `vercel.json` cron set to daily as a backup.
- **Health Auto Export** (iOS, HealthyApps) — bridges Apple HealthKit → `/api/health-import` via REST POST. Premium required for automations. **Must be set to daily aggregation** to match the one-row-per-metric-per-day schema.

**Env vars added:**
- `REMINDERS_CRON_SECRET` — authenticates cron-job.org → `/api/cron/reminders`. ⚠️ Rotate: value was partially visible in a screenshot.
- `HEALTH_IMPORT_SECRET` — authenticates Health Auto Export → `/api/health-import`. Set in Vercel Production + redeploy before HAE pipeline is live.

**Installed Claude Code skills (in repo):**
- `ui-ux-pro-max` — design-system generator; auto-fires on any UI/UX work. **Clashes with Loam and Glow** — accepted on trial; steer back toward Loam and Glow, scope down if it fights.
- `hyperframes` (HeyGen) — HTML→MP4 video rendering; 15 skills, installed as symlinks between `.agents/skills/` and `.claude/skills/` (verify symlinks survive clean clone / Vercel build).
- `graphify` — `/graphify` repo knowledge graph. Run `graphify update .` after code changes (no API cost). Current to HEAD `1c4c94d`.

**MCP servers:**
- 21st.dev **Magic** — user/global scope; UI component library. Key at `C:\Users\Phili\.claude.json` → `mcpServers.magic.env.API_KEY`. ✅ Rotated session 3.
- **ruflo** — project scope, multi-agent meta-harness; connected via `npx`. Heavily over-scoped for a solo single-repo project — candidate to remove if it isn't earning its place.

---

## 3. Pillars / scope (6)

1. **Organisation** — Tasks, Projects, People, Captures, Decisions, Purchases, **Habits**. Routes: `/organisation/*` (renamed from `/compost/*`; redirects in place).
2. **Fitness** — Workouts, Programmes, Exercise History/Aliases, Baselines, Pain Logs, Workout Now. Routes: `/fitness/*`. Horizontal sub-nav: Today / Programmes / History / Body.
3. **Health** — Nutrition, Supplements, Body Metrics (canonical; moved from Fitness). Drafts: Gut Health, Blood Tests, Recipes. Routes: `/health/*`. Apple Health pipeline code-done, not active.
4. **Brain** — Journal, Reminders, Calendar, search/AI. Drafts: Watchlist, Places. Routes: `/brain/*` (renamed from `/stroma/*`; redirects in place).
5. **Finance** — Spending (multi-bank), Net Worth, Fuel, Analysis. Draft: Accounts register. Routes: `/finance/*`.
6. **Studio** — Music, Footage, Design/Video, PC Build. Routes: `/studio/*`. Placeholder live; data models TBD.

---

## 4. Build queue (in order)

1. ~~Polish pass~~ ✅ done
2. ~~Tickers + branding~~ ✅ done — rotating top-rail: BTC/ETH/USDT, NDX/SPX, Gold/Silver/Copper/Palladium/Oil; 60s polling; CoinGecko + Yahoo Finance; sunrise widget; MYCELIUM rebrand. (`4f1de0f`)
3. ~~Finance privacy toggle~~ ✅ done — original blur (`2930edf`); upgraded to true redaction via `<Money>` + `<PrivateText>`, constant-width placeholder, default-hidden session-only (`d1629a2`, `7f3e80b`).
4. ~~UK fuel prices~~ ✅ done — CMA data, Armthorpe + 5mi radius, ticker + home card. (`8f3124d`)
5. **Spending tracking** ✅ Phase A + B + D complete. A: Halifax/Revolut/AMEX/PayPal imports, multi-bank schema, date-range presets. B: type normalisation (0047), AI categorisation (0048), rules engine, txn_agg RPC. D: migration 0050 (spend_by_category + spend_by_month RPCs), `/finance/analysis` page (period selector, summary cards, category bar chart, monthly stacked trend), mini overview panel on spending page. **Still to do: C (other banks).**
6. **Live finances (later)** — Coinbase + Rabby + Revolut Stocks holdings via Yahoo Finance; banks manual. Revolut current account not viable without paid Open Banking.

---

## 5. Active work & open items  _(VERIFY — most likely to be stale)_

**Built (session 4 — 8–9 June):**
- **Dashboard as home** ✅ — `/` = card grid (was `/dashboard`), `/hub` = Canvas rhizomorph hub. `/dashboard` redirects to `/`. Commits: c0008d5, a282de8.
- **TendrilSpine removed** ✅ — replaced by TopRail on all section pages.
- **TopRail rewritten** ✅ — 7 tabs: Dashboard / Organisation / Fitness / Health / Brain / Finance / Studio + Review. Hamburger on mobile. MYCELIUM wordmark links to `/hub`. MYCELIUM colour-cycle animation (14s, all 6 section colours) on wordmark.
- **CSS Grid dense masonry dashboard** ✅ — 12-col grid, `grid-auto-flow: row dense`, 10 card sizes (sm/md/tall/wide/lg/full/tall-3/tall-4/md-3/md-4), localStorage persistence, edit mode with drag handles + size picker, responsive (6-col tablet, stacked mobile).
- **Spending Phase D** ✅ — migration 0050. `spend_by_category` + `spend_by_month` RPCs. `/finance/analysis`: period selector, 4 summary cards, horizontal bar chart by category, stacked monthly trend (last 6 months). Mini collapsible overview panel on spending page. Commits: 078c4ef, 95aedc2.
- **Route renames** ✅ — `app/compost/` → `app/organisation/`, `app/stroma/` → `app/brain/`. 19 files moved, 31 references updated. Backwards-compat redirects kept. Commit: 959714d.
- **Fitness sub-nav** ✅ — horizontal tab bar in fitness layout: Today / Programmes / History / Body.
- **Brain TRY prompts removed** ✅ — example query suggestions removed from ask page.
- **Session time editable** ✅ — finish session modal has h/m number inputs; back-calculates `completed_at` from `started_at + duration_minutes`.
- **Habits page** ✅ — `/organisation/habits`. Toggle tiles (full-page), 1-year heatmap (CSS Grid column-flow, 53 cols, 4 amber opacity tiers, hover tooltips, month labels), per-habit breakdown table (streak/30d/90d). Reads from `daily_logs.notes` JSON — no migration needed. Card added to Organisation landing page.
- **Review nav item** ✅ — TopRail tab linking to `/organisation/review`.

**Active HEAD:** post-72b629b (habits + heatmap commits pushed after, hashes not recorded)

**Built (session 3 — 7–8 June, migrations 0047–0048):**
- **Type standardisation ✅** — migration 0047.
- **Spending Phase B ✅** — migration 0048. Rules → AI (Haiku) → manual. Commits: 6377f43, de29e73.
- **Homescreen remodel ✅** — rhizomorph cluster hub at `/hub`. Commits: 1c4d34, 34bbd5, 41daeda+.

**Built (session 2 — 6 June, migration 0046):**
- **Reminders page** ✅ — `/reminders` (`d0feedd`).
- **Exercise aliases** ✅ — migration 0046 (`5886f43`).
- **Six draft scaffolds** ✅ — `/draft/{recipes,gut-health,blood-tests,accounts,studio,watchlist}` (`3dae355`).

**Outstanding setup (manual — keeps getting deferred):**
- `HEALTH_IMPORT_SECRET`: add to Vercel Production + redeploy.
- **Health Auto Export app**: install + configure; daily aggregation; `apple-health-setup-guide.md` in session outputs.
- **REMINDERS_CRON_SECRET**: rotate (partially visible in screenshot).

**Deferred:** PDF statement import; browser automation for bank exports; fuel error (parked).

---

## 6. Fitness spec

**Status: R1 through R4 + R3a/b/c all shipped; exercise aliases shipped.**

- **Data model:** `workout_programmes` + phases (`start_week_iso`/`end_week_iso`), `workout_programme_sessions` (day, slot, kind), `workout_programme_exercises`. Logs: `workout_sessions`, `_session_exercises`, `_sets`.
- **Defaults:** morning = cardio, afternoon = resistance, default rest 90s. Lifts use kg | lbs | stone (3-unit toggle per exercise, stone default for body metrics). A day log = snapshot of the template at log time. Save-to-template checkbox on ad-hoc. Auto-save 500ms. PPL programme seeded from Phil's HTML.
- **Templates are the single source of truth** — programmes reference templates with live sync.
- **R3b — pain tracking ✅:** `exercise_baselines` seeded from `injury_tracker.xlsx`; `exercise_pain_logs` table; opt-in; progression chart colour-codes by pain + separate pain-over-time chart. (`128b0a0`, `2df8558`, `7699ad8`)
- **R3c ✅:** voice + iOS Shortcut pain logging + standalone `pain_log` kind. (`78bfa34`)
- **Phase 2 ✅:** 4 slots, multiple sessions per slot (UNIQUE dropped). Kinds: cardio/conditioning/resistance/mobility. Per-exercise shape; weight optional; voice ambiguity → picker; auto-add empty evening to weekdays; conditioning chip + icon. (`3f43aa8`)
- **R4 ✅:** `/fitness today` day-swap dropdown; "+ Add extra session today"; session-type tags; drag-to-reorder exercises within session. (`bd3d2c6`, `baf62a5`, `4ae74d0`)
- **Body metrics ✅:** manual logging via `/fitness/body`; Apple Health webhook bridge + manual fallback. (`e8c1a85`)
- **Workout Now + offline ✅:** `/workout-now` PWA; IndexedDB queue + Background Sync; localStorage template cache; idempotent upsert by client UUID. (`62dd1da`, `e92dc34`, `a7759b0`)
- **Exercise aliases ✅:** `exercise_aliases` table + `resolve-aliases.ts`; alias union at query time across history/baselines/pain-logs; CRUD + collapsible UI on history page. (`5886f43`)
- **Multi-user (backlog):** open fitness to other users (needs user-scoping/auth). Per-set timing.

---

## 7. Backlog (other features)

**Done — previously in backlog:**
- ~~Compost (CRM rename)~~ ✅ — `/compost/{tasks,captures,people,decisions}`; kanban, table, calendar, split-pane detail, comments + activity log, bulk actions, shortcuts, drag-to-schedule, context-switcher, NOW filter, kind conversion. (`05ca8f1`+)
- ~~Projects in Compost~~ ✅ (`e377216`, `56517c9`, `0668db1`)
- ~~Purchases~~ ✅ (`50fb377`, `bdd7314`, `0668db1`)
- ~~Movable dashboard cards~~ ✅ (`b5f53e4`, `020628a`, `68b13c0`, `c8ef0a9`, `1f3aec3`)
- ~~People overhaul~~ ✅ (`d849074`)
- ~~Capture review page~~ ✅ (`5d848a7`, `7804312`, `bf49cc7`)
- ~~Voice-routing rules viewer/editor~~ ✅ (`795c763`, `6cabd8c`, `9530f2f`)
- ~~Glossary dashboard card~~ ✅ (`1c55986`)
- ~~Brain → Stroma rename~~ ✅ (`baab30d`)
- ~~Entity review rules~~ ✅ (`e81a35b`, `7804312`, `1b5a82e`)
- ~~iOS Shortcut import (partial)~~ ✅ (`3f9bcbb`)
- ~~PWA + push notifications~~ ✅ (`b463ba7`, `2025fe4`, `8997370`)
- ~~Morning briefing~~ ✅ (`1f238b1`)
- ~~Voice journal~~ ✅ (`4ce5af5`)
- ~~Section overview pages~~ ✅ (`cabd137`)
- ~~Stroma integrations page~~ ✅ (`be51c4d`)
- ~~Sub-tasks~~ ✅ (`7292ae1`)
- ~~Supplement tracker~~ ✅ (session 1)
- ~~PC build log~~ ✅ (session 1)
- ~~Places + map~~ ✅ (session 1)
- ~~Reminders + smarter capture brain (partial)~~ ✅ — outbound Telegram, `reminders` table, cron pipeline, NL parsing. Still pending: broader `classifyCapture.ts` intent expansion.
- ~~Reminders page~~ ✅ — `/reminders` (session 2, `d0feedd`).
- ~~Exercise aliases~~ ✅ — (session 2, `5886f43`).

**Still pending:**
- **Spending tracking C** — other banks beyond Halifax/Revolut/AMEX/PayPal.
- **Live finances** — Coinbase + Rabby + Revolut Stocks via Yahoo Finance. Banks manual.
- **iOS Shortcut ⇄ Telegram full bidirectional parity** — expose `POST /api/fitness/pending-routes/[id]/resolve`; add `button_options` to `/api/capture-audio` response; Shortcut uses Choose-from-List + banner. v1 = workout picker only.
- **`classifyCapture.ts` intent expansion** — broader NL intent handling beyond reminders.
- ~~Homescreen remodel~~ ✅ — rhizomorph hub at `/hub`, card grid dashboard at `/`, TopRail nav (sessions 3–4).
- **Multi-user fitness** — user-scoping + auth. Per-set timing.
- **Investment purchase tracker** — tag/kind for investment buys; price research + monitoring; eBay sold / StockX; `price_watch` table with scheduled checks.
- **DB backup system** — nightly `pg_dump` off-site (S3/R2), retention 7d/4w/12m, restore test every 90d. First check if Supabase plan already includes automated backups.
- **Mobile testing pass** — rigorous real-iPhone pass once features are stable.
- **Apple Health pipeline active** — code done; needs HAE app setup + `HEALTH_IMPORT_SECRET` + redeploy.

**Draft scaffolds awaiting design sign-off (built session 2, URL-only, no persistence):**
- **`/draft/recipes`** — recipe library + weekly meal-planner grid + shopping-list stub. Real version needs OCR-of-recipe-cards pipeline + Telegram shopping-list send.
- **`/draft/gut-health`** — daily log + Bristol Stool Scale 1–7 + timeline. NO trigger-likelihood scoring. Separate mental-clarity surface planned.
- **`/draft/blood-tests`** — markers-over-time table + per-marker trend placeholder. Schema decision (markers/units/ranges) unresolved.
- **`/draft/accounts`** — service register (name, email, purpose, status, opened date, optional cost, notes). NO passwords/2FA. Open question: fold into a contracts model with billing fields.
- **`/draft/studio`** — three-tab placeholder (Music / Footage / Design-Video). Three distinct data models not yet decided; Design/Video may overlap with Projects in Compost.
- **`/draft/watchlist`** — combined to-watch / to-read, grouped by status with ratings. End goal: feed Claude recommendations.

**Other backlog (need design):**
- **Food preferences + intolerance data** — feeds recipe filtering.
- **Eye prescription** — simple Health record.
- **Asset inventory** — spun out as its own project/chat. Possibly a sellable product.
- **PC cleanup project** — Mycelium holds plan/checklist + directory conventions only.
- **File-organisation project** — Mycelium holds plan/conventions.
- **Gaming section (DEFERRED)** — PC mod manager + upcoming-releases tracker. Remind Phil when he next discusses the backlog.
- **Expert-agent builder** — on-site persistent expert agents grounded in Phil's data. Deferred until enough structured data exists.
- **Social media content studio (discussed, not scoped)** — draft/improve posts; business page scheduling via Instagram Graph API. Consuming friends' feeds not possible via official APIs.

---

## 8. Design system — "Loam and Glow" (LOCKED)

- **Palette:** ink-0 `#0e1410` → ink-4 `#3a4639`; text-0 `#e8e6dd` → text-3 `#4d4b43`; glow-0 `#84f5b8` (hero/active moments only).

**Section accent colours (locked):**
| Section | Accent | Notes |
|---|---|---|
| Dashboard | `#e8e6dd` | Warm white — neutral overview |
| Fitness | `#84f5b8` | Primary glow — the main organism colour |
| Finance | `#6db8f5` | Cool blue — crystalline, precise |
| Health | `#5de8e0` | Teal — distinct from Finance and Fitness |
| Organisation | `#f5b56d` | Amber — existing warn colour, earthy |
| Studio | `#f56db5` | Magenta — creative register |
| Brain (hub centre) | `#84f5b8` | Shares primary glow — the organism's core |

**Navigation architecture (locked):**
- `/` = card grid dashboard (home). `/hub` = Canvas rhizomorph hub (accessible via MYCELIUM wordmark in TopRail).
- Hub: dense L-system Brain core → 10-strand tapered rhizomorph bundles (Brain green `#84f5b8` → section colour gradient) → dense L-system cluster orbs as section nodes. Forest floor background (`/public/images/forest-bg.jpg`, overlay rgba(5,8,6,0.52)). Phased growth animation: dark → core → rhizomorphs → clusters → labels.
- Entry animation: bloom transition (clip-path circle) 480ms → navigate → 400ms reveal. TransitionContext in `lib/context/`. Imperative DOM reflow for CSS trigger.
- **TopRail** (all pages): Dashboard / Organisation / Fitness / Health / Brain / Finance / Studio / Review. Hamburger collapse on mobile. MYCELIUM wordmark → `/hub`; 14s colour-cycle animation (all 6 section colours).
- Fitness: horizontal sub-nav tabs in layout (Today / Programmes / History / Body).
- TendrilSpine removed.

- **Type:** Inter Tight (UI), Recoleta (wordmark + H1). Scale 12–64. Radius 4/8/16/24.
- **Wordmark:** "MYCELIUM ·" with pulsing spore-dot.
- **Icons (5 bespoke):** fruiting body, hyphal network, spore burst, mat, fiddlehead.
- **Motion:** growth, glow, optional connection threads.
- **Full theming pass deferred** until after fitness R3 + spending Phase A + ~2 weeks real use. Direction: dark earthy base, bioluminescent accent, branching motifs, organic type — avoid cartoon mushrooms / over-mimicry.

---

## 9. Principles & learnings

- **CSS animations are unreliable for complex backgrounds** — HyphalThreads rewritten SVG/CSS → Canvas. Canvas is the pattern for motion-heavy backgrounds.
- **Workout templates = single source of truth;** day logs are snapshots.
- **Voice captures are context, not data** — for finances especially, CSV is canonical and voice annotates/reconciles.
- **Entity review for automated captures** — voice/Telegram-created entities route to review; UI-created bypass.
- **Open Food Facts:** UK subdomain, English forced; USDA secondary for raw ingredients; Claude Vision OCR fallback for label scanning.
- **Diagnose first, then fix** — after shotgun fixes caused regressions, send a diagnose-first prompt before fixing batches of bugs.
- **True redaction > CSS blur** — never render the secret value in the DOM when hidden; constant-width placeholder; default hidden, session-only reveal.
- **Migration numbering by BUILD ORDER, not feature label** — assign next available number at build time; out-of-order numbers break `supabase db push`.
- **Daily trackers must boundary on Europe/London, not UTC** — BST shifts "today" by an hour; use `toLocaleDateString("en-CA", { timeZone: "Europe/London" })`.
- **One-tap capture needs an undo/delete path** — no-confirm actions create permanent phantom data that erodes trust.
- **Apple Health has no web API** — bridge = Health Auto Export iOS app → REST POST to `/api/health-import`; phone must be unlocked; set to daily aggregation.
- **Vercel cron on Hobby: once/day max** — sub-daily expressions fail the deploy. Workaround: `vercel.json` cron daily as backup; cron-job.org drives sub-daily (free, custom headers, every-minute resolution).
- **App-wide session middleware blocks external machine API callers** — exempt only specific paths; confirm each exempted route enforces its own bearer-token auth.
- **OpenAI Whisper 429 `insufficient_quota` = billing state** — check platform.openai.com → Billing. Not a code bug.
- **Voice capture failure must never silently drop a capture** — reply in Telegram on error + preserve `file_id` for retry.
- **React 19 purity is stricter than 18** — `Date.now()` anywhere in the render path (incl. `useMemo`/`useRef` initialisers) is a build error; fix with `useState(() => Date.now())` (initialisers are exempt). Don't call a state-setting `useCallback` from inside an effect; inline the async fetch as an IIFE with a cancellation flag.
- **Supabase `.or()` ilike filters break on commas/periods in values** — those are the filter delimiters; sanitise alias/input strings before building `.or()` strings. (Latent — matters when the fuller alias management UI lands.)
- **PostgREST silently truncates at 1000 rows** — `.limit(n)` client-side is ignored; the REST layer caps at 1000. Fix: server-side PostgreSQL aggregate function (`txn_agg`) bypasses the cap. Any server-side sum/count over a large table needs this treatment.
- **IS DISTINCT FROM vs !=** — `!=` excludes NULLs; `IS DISTINCT FROM 'x'` treats NULL as "not x". Use `IS DISTINCT FROM` when filtering categories that may be unset.
- **CSS transitions need a forced reflow** — setting React state that applies the end value directly doesn't trigger animation (React batches it). Fix: imperative DOM access after `void el.offsetHeight`. Correct use of DOM manipulation in React (TransitionOverlay).
- **Catmull-Rom via quadratic midpoints** — smooth closed organic curves: move to midpoint between point 0 and point 1, then `quadraticCurveTo` through each successive point to the next midpoint. Continuous curves that look irregular without being spiky.
- **Growing thread animation** — progressive bezier draw (t=0 to growT, step dt=0.02, loop) rather than `setLineDash`; gives full control over tip effects and secondary branching timed to growT.
- **`grid-auto-flow: row dense` fills masonry gaps** — CSS Grid handles irregular card sizes without JS; source order = placement priority. No library needed.
- **`useSyncExternalStore` for breakpoint detection** — SSR-safe, avoids hydration mismatch vs `useEffect`+`useState` pattern.
- **`createLinearGradient` on Canvas strands** — Brain green → section colour gradient along each filament; set gradient coords from node to cluster centre.
- **Contribution heatmap layout** — `gridAutoFlow: "column"`, `gridTemplateRows: repeat(7, 1fr)`, fixed container height. Percentage-positioned month labels (`left: ${col/total*100}%`) scale with fluid grid; no pixel-width knowledge needed.
- **`queueMicrotask(() => setState(null))`** — defers state reset to avoid React 19 cascading render warning when clearing after an optimistic update.
- **Duration trust + back-calculation** — "trust the user" duration override in finish-session modal: back-calculate `completed_at = started_at + duration_minutes`. Don't block save on impossible combos; accept and store.
- **Heatmap rate calculation guard** — when switching from 90-day to 1-year API fetch, ensure any "last-N-days" rate still filters by `daysAgo < N`; don't count the full history set.

---

## 10. Naming / metaphor (locked)

Mycelium (organism) → **Organisation** (CRM / task space, routes `/organisation/*`; internal component dirs still named `compost`), **Brain** (routes `/brain/*`; was `stroma`). Mycological metaphor is core; the name "Mycelium" is locked.