export const NUTRITION_TARGETS = {
  kcal: 2800,
  protein: 180,
  carbs: 300,
  fat: 80,
};

// Local-time hour after which the UI nudges the user that they've passed
// their eating cutoff. Soft warning only — logging still works after this.
export const CUTOFF_HOUR = 17;
